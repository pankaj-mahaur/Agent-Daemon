node_modules/
__pycache__/
.venv/
*.pyc
.DS_Store
Thumbs.db
.graphify_*
graphify-out/
*.log

# Claude Code session artifacts (worktrees, logs)
.claude/

# agent-daemon runtime state (per-machine)
.agent-daemon/

# Local temp / IDE state
.vscode/
.idea/
*.swp
.env
.env.local

# Vendored upstream repos: keep MANIFEST + fetch script, ignore the clone itself
vendored/*
!vendored/MANIFEST.md
!vendored/fetch.mjs
!vendored/.gitkeep

# Internal working docs — local-only, not for the public repo
HANDOFF.md
FUTURE-*-PLAN.md
docs/onboarding.html
hello-world.js

